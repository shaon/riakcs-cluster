source "https://supermarket.getchef.com"

metadata

group :integration do
  cookbook "apt"
  cookbook "yum"
  cookbook "sudo"
  cookbook "minitest-handler"
  cookbook "packagecloud"

  cookbook "riak-cs", github: "basho-labs/riak-cs-chef-cookbook"
end
